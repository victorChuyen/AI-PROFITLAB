// OPC AI PROFITLAB — Site Configuration
// Domain: go.breaths.live
// Set only after checking the real product, price, payment and delivery flow.
window.OPC_SITE = Object.freeze({
  checkoutUrl: '',
  checkoutVerified: false,
  liveScheduleText: ''
});
